apiVersion: v1
kind: Service
metadata:
    name: "masterarbeitcluster-a0b1"
    labels:
        app: "masterarbeitcluster-a0b1"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "masterarbeitcluster-a0b1"